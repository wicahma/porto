export type TChip = {
    id: number;
    label: string;
    active: boolean;
};

export type TExperience = {
    id: number;
    isNew: boolean;
    text: string;
};

export type TCustom = {
    i: number;
};
